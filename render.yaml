services:
  - type: web
    name: auto-caller-crm
    env: static
    buildCommand: npm install && npx vite build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
